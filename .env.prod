APP_NAME="SOS Mulher Real"
APP_ENV="production"
APP_KEY="base64:4XLjw5Ikm3nBPG/1VOUHDcByPh9LVEmdWUmzdeaiQno="
APP_DEBUG="true"
APP_URL="https://sosmulherrealweb-production.up.railway.app"
FORCE_HTTPS="true"

# Database
DB_CONNECTION="mysql"
DB_HOST="crossover.proxy.rlwy.net"
DB_PORT="43901"
DB_DATABASE="railway"
DB_USERNAME="root"
DB_PASSWORD="dIBCgsvvrkZLpPAtKmNRlgNvQzoqoYdM"

# Redis para TUDO (incluindo broadcast)
BROADCAST_DRIVER=redis
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis

REDIS_HOST="${REDISHOST}"
REDIS_PASSWORD="${REDISPASSWORD}"
REDIS_PORT="${REDISPORT}"

# Configurações do Laravel Echo
LARAVEL_ECHO_SERVER_AUTH_HOST="https://sosmulherrealweb-production.up.railway.app"
LARAVEL_ECHO_SERVER_REDIS_HOST="${REDISHOST}"
LARAVEL_ECHO_SERVER_REDIS_PORT="${REDISPORT}"
LARAVEL_ECHO_SERVER_REDIS_PASSWORD="${REDISPASSWORD}"

# Configurações para o frontend
PUSHER_APP_ID="sos-mulher-real"
PUSHER_APP_KEY="sos-mulher-real-key"
PUSHER_APP_SECRET="sos-mulher-real-secret"
PUSHER_HOST="sosmulherrealweb-production.up.railway.app"
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"